# 思路：白名单 —— 默认忽略所有文件，只放行明确列出的源码/文档。
# 好处：nvcc 编译出的可执行文件没有扩展名（如 unified_memory_manualBest），
# 用 *.o 之类的黑名单抓不住；白名单则天然覆盖一切编译产物。

# 1. 默认忽略一切
*

# 2. 放行目录本身（否则无法进入子目录匹配下面的规则）
!*/

# 3. 放行源码与文档
!*.cu
!*.c
!*.cpp
!*.h
!*.hpp
!*.md

# 4. 放行构建脚本与 git 自身
!Makefile
!makefile
!CMakeLists.txt
!.gitignore
